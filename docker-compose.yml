services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: rechorder
      POSTGRES_USER: rechorder
      POSTGRES_PASSWORD: rechorder
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rechorder -d rechorder"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
